--- a/comfy/sd.py
+++ b/comfy/sd.py
@@ -1,3 +1,4 @@
+import fc_trace3 as _t3
 import json
 import torch
 from enum import Enum
@@ -121,6 +122,7 @@
     if clip is not None:
         new_clip = clip.clone()
         k1 = new_clip.add_patches(loaded, strength_clip)
+        _t3.lora("comfy", strength_clip, k1, loaded)
         if lora_metadata:
             new_clip.patcher.set_attachments("lora_metadata", lora_metadata)
     else:
